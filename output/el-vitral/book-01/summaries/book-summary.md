# El Apagado — running summary

> Synthesis rebuilt by `close-act` at act boundaries. Between acts, only
> `## What just happened` is touched per chapter (by `update-canon`) so a
> fresh `resume-act` mid-act re-enters cleanly.

## What just happened

- (aún sin capítulos escritos; se rellena con `update-canon` a partir del cap 1.)
